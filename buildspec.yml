version: 0.2
    
phases:
  install:
    runtime-versions:
      dotnet: 5.0
      #dotnet: latest - Latest resolve to 3.1, check this issue https://github.com/aws/aws-codebuild-docker-images/issues/414
   
   build:
    commands:
      - echo Publish started on `date`
      # https://docs.aws.amazon.com/elasticbeanstalk/latest/dg/dotnet-linux-platform-bundle-app.html
      - dotnet publish -c Release -r linux-x64 -o ./ClinicManagement/ClinicManagement.csproj
 # build:
   # commands:
      - echo Build started on `date`
      - dotnet build ./ClinicManagement/ClinicManagement.csproj
      # echo Test started on `date`
      # dotnet test ./ClinicManagement/ClinicManagement.csproj
      
  #post_build:
      
artifacts:
  files:
    - ./**/*
